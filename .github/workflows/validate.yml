name: Validate

on:
  push:
  pull_request:
  schedule:
    - cron: '0 3 * * *'

jobs:
  hacs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: hacs/action@main
        with:
          category: plugin
